name: keepwarm
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSf --max-time 90 "${{ secrets.SNAKE_URL }}" || true
